#ifndef ALLINTERESTS_H
#define ALLINTERESTS_H

#include <string>
#include <vector>
using namespace std;

// Declare the global vector for all interests
extern vector<string> allInterests;

// Function to display all interests
void displayAllInterests();

#endif // ALLINTERESTS_H
